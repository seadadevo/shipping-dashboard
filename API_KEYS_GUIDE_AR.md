# 🔑 دليل الحصول على مفاتيح API للذكاء الاصطناعي

## ✅ المشكلة تم حلها!

تم إصلاح مشاكل الحزم (Packages) التي كانت تمنع الـ AI من العمل.
السيرفر الآن يعمل بنجاح! ✅

## ⚠️ الخطوة المطلوبة الآن: الحصول على API Key

الـ AI يحتاج **OpenRouter API Key** للعمل. اتبع الخطوات التالية:

---

## 📝 الخطوات بالتفصيل

### 1️⃣ الذهاب إلى موقع OpenRouter

افتح المتصفح واذهب إلى:

```
https://openrouter.ai/
```

### 2️⃣ إنشاء حساب

- اضغط على **Sign Up** أو **Log In**
- يمكنك التسجيل بالـ Google Account أو البريد الإلكتروني
- أكمل عملية التسجيل

### 3️⃣ الانتقال إلى إعدادات الـ API Keys

بعد تسجيل الدخول:

1. اضغط على صورة الملف الشخصي في الأعلى
2. اختر **Settings** (الإعدادات)
3. اذهب إلى تبويب **Keys** (المفاتيح)

### 4️⃣ إنشاء API Key جديد

1. اضغط على **Create Key** أو **New API Key**
2. سيظهر لك مفتاح يبدأ بـ `sk-or-v1-...`
3. **IMPORTANT:** انسخ المفتاح فوراً! لن تستطيع رؤيته مرة أخرى
4. احتفظ به في مكان آمن

### 5️⃣ وضع المفتاح في الـ .env

افتح ملف `.env` الموجود في مجلد `server`:

```
shipping-dashboard/server/.env
```

ابحث عن السطر:

```env
API_KEY=YOUR_OPENROUTER_API_KEY_HERE
```

استبدله بـ:

```env
API_KEY=sk-or-v1-your-actual-key-here
```

**احذف** `YOUR_OPENROUTER_API_KEY_HERE` وضع المفتاح الحقيقي بدلاً منه.

### 6️⃣ إعادة تشغيل السيرفر

أوقف السيرفر (Ctrl+C) ثم شغله من جديد:

```bash
cd server
npm start
```

---

## ✅ كيف تعرف أن كل شيء يعمل؟

عند بدء السيرفر، يجب أن **لا** ترى هذه الرسالة:

```
❌ ERROR: API_KEY not found in .env file!
```

إذا رأيت فقط:

```
Server running on port 5000
MongoDB Connected to Atlas!
```

**تهانينا!** 🎉 الـ AI جاهز للاستخدام.

---

## 🧪 اختبار الـ AI

### من واجهة Dashboard:

1. اذهب إلى صفحة **AI Mode**
2. اكتب سؤال مثل: "كيف أنشئ طلب شحن؟"
3. يجب أن يرد عليك الـ AI بالعربي

### من Terminal:

```bash
# اختبار بسيط
curl -X POST http://localhost:5000/api/ai/chat \
  -H "Content-Type: application/json" \
  -d "{\"question\":\"ما هو هذا النظام؟\"}"
```

---

## 💰 التكلفة

OpenRouter يعطيك **$1 رصيد مجاني** عند التسجيل.

هذا كافي لـ:

- حوالي **25,000 رسالة** مع GPT-4o-mini (الموديل المستخدم)
- اختبار الـ Project بالكامل
- عمل عدة Demos

بعد انتهاء الرصيد المجاني، يمكنك شراء رصيد إضافي بسعر رخيص جداً.

---

## ❓ المشاكل الشائعة

### المشكلة: "API_KEY not configured"

**الحل:** تأكد من أنك وضعت المفتاح في `.env` بدون مسافات وبدون علامات تنصيص.

### المشكلة: "Invalid API Key"

**الحل:**

- تأكد أنك نسخت المفتاح بالكامل من OpenRouter
- تأكد من عدم وجود مسافات قبل أو بعد المفتاح
- جرب إنشاء مفتاح جديد

### المشكلة: "Request failed"

**الحل:**

- تأكد من اتصالك بالإنترنت
- تأكد من أن رصيدك المجاني لم ينته

---

## 📚 معلومات إضافية

### ما هو OpenRouter؟

OpenRouter هي خدمة توفر الوصول لنماذج AI مختلفة (GPT-4, Claude, Gemini) من خلال API واحد موحد.

### لماذا نستخدم OpenRouter؟

- أسعار أرخص من OpenAI مباشرة
- رصيد مجاني للاختبار
- دعم موديلات متعددة
- سهولة الاستخدام

### الموديلات المستخدمة في المشروع:

1. **text-embedding-3-small** - لتحويل النصوص إلى Embeddings
2. **gpt-4o-mini** - للإجابة على الأسئلة والتحليل

---

## 🎓 ملاحظات للـ Graduation Project

عند تقديم المشروع:

- احتفظ بـ API Key خاص في ملف `.env` منفصل
- **لا تشارك** الـ API Key على GitHub أو Discord
- يمكنك إنشاء Key جديد خاص بكل Demo
- احذف Keys القديمة بعد انتهاء التقديم

---

## 📞 هل تحتاج مساعدة؟

إذا واجهت أي مشكلة بعد وضع الـ API Key، تحقق من:

1. السيرفر شغال على `http://localhost:5000`
2. لا توجد أخطاء في Console
3. الـ MongoDB متصل

---

**Created:** 2025
**Project:** Shipping Dashboard with AI Assistant
**Course:** Graduation Project - ITI
